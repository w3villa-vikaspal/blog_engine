source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in blog_engine.gemspec.
gemspec

group :development do
  gem 'mysql2', '~> 0.5.2'
end

# To use a debugger
# gem 'byebug', group: [:development, :test]
